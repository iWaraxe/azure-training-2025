apiVersion: v1
kind: Service
metadata:
  name: {{ include "mars-application.fullname" . }}
  labels:
    {{- include "mars-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mars-application.selectorLabels" . | nindent 4 }}
